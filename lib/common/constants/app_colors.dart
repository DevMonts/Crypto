import 'dart:ui';
import 'package:flutter/material.dart';

class AppColors {
  static const Color bitcoinColor = Color(0xFFF7931A);

  static const Color transparent = Color.fromRGBO(255, 255, 255, 1);

  static const Color greenIfOpacity = Color.fromRGBO(0, 255, 0, 0.25);
  static const Color green = Colors.green;

  static const Color redIfOpacity = Color.fromRGBO(255, 0, 0, 0.25);
  static const Color red = Colors.red;
}
